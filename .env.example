# 관리자 로그인 (복사 후 .env.local 로 저장하고 값 입력 — .env.local 은 Git 에 올리지 마세요)
VITE_ADMIN_ID=
VITE_ADMIN_PASSWORD=

# 1 = 서버(Flask) data/channel_groups.json 중앙 저장. 로컬 npm run dev 시에는 deploy/pythonanywhere 에서 flask run -p 5000 필요
#VITE_CENTRAL_CHANNELS=1
